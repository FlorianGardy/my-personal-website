import React from "react";

const CV = props => {
  const cvStyle = {
    section: {
      width: "100%"
    }
  };
  return (
    <section style={cvStyle.section}>
      <h2>Mon curriculum vitae</h2>
      <p>... mon CV ...</p>
    </section>
  );
};

export default CV;
